dist: trusty
sudo: require

language: cpp
compiler: g++

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-5
      - g++-5
      - cmake

script:
  - gcc -v
  - g++ -v
  - cmake --version
  - ./wmake test

